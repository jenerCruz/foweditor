"use client";
import { __rest, __assign } from '../_virtual/_tslib.js';
import classNames from '../external/classnames/index.js';
import React__default from 'react';
import styles from './text.module.css.js';

function Text(props) {
    var _a;
    var className = props.className, _b = props.variant, variant = _b === void 0 ? "body" : _b, ellipsis = props.ellipsis, _c = props.color, color = _c === void 0 ? "default" : _c, otherProps = __rest(props, ["className", "variant", "ellipsis", "color"]);
    return (React__default.createElement("span", __assign({ className: classNames(className, (_a = {},
            _a[styles.label] = variant === "label",
            _a[styles.labelBold] = variant === "label-bold",
            _a[styles.body] = variant === "body",
            _a[styles.bodyBold] = variant === "body-bold",
            _a[styles.subheader] = variant === "subheader",
            _a[styles.sectionHeader] = variant === "section-header",
            _a[styles.monospaceBody] = variant === "monospace-body",
            _a[styles.header] = variant === "header",
            _a[styles.ellipsis] = ellipsis,
            _a[styles.colorSubtext] = color === "subtext",
            _a)) }, otherProps)));
}

export { Text };
