"use client";
import { __rest, __assign } from '../_virtual/_tslib.js';
import React__default from 'react';
import * as index from '../assets/icons/final/index.js';
import { IconWrapper } from './icon-wrapper.js';

var Icon = React__default.forwardRef(function Icon(props, ref) {
    var name = props.name, otherProps = __rest(props, ["name"]);
    if (name === null) {
        return null;
    }
    if (name === "empty") {
        return (React__default.createElement(IconWrapper, __assign({ ref: ref }, otherProps),
            React__default.createElement("svg", { width: "1em", height: "1em" })));
    }
    var Component = index[name];
    // TODO warn?
    if (!Component) {
        console.warn("Icon rendered with invalid name: ".concat(name));
        return React__default.createElement(IconWrapper, __assign({ ref: ref }, otherProps));
    }
    // Icon components are already wrapped with an IconWrapper internally.
    return React__default.createElement(Component, __assign({ ref: ref }, otherProps));
});

export { Icon };
