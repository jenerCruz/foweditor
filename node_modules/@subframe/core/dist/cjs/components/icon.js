"use client";
'use strict';

var _tslib = require('../_virtual/_tslib.js');
var React = require('react');
var index = require('../assets/icons/final/index.js');
var iconWrapper = require('./icon-wrapper.js');

var Icon = React.forwardRef(function Icon(props, ref) {
    var name = props.name, otherProps = _tslib.__rest(props, ["name"]);
    if (name === null) {
        return null;
    }
    if (name === "empty") {
        return (React.createElement(iconWrapper.IconWrapper, _tslib.__assign({ ref: ref }, otherProps),
            React.createElement("svg", { width: "1em", height: "1em" })));
    }
    var Component = index[name];
    // TODO warn?
    if (!Component) {
        console.warn("Icon rendered with invalid name: ".concat(name));
        return React.createElement(iconWrapper.IconWrapper, _tslib.__assign({ ref: ref }, otherProps));
    }
    // Icon components are already wrapped with an IconWrapper internally.
    return React.createElement(Component, _tslib.__assign({ ref: ref }, otherProps));
});

exports.Icon = Icon;
